/**
 * 用户性别枚举。
 */
export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
  SECRET = 'secret',
}

/**
 * Redis 存储命名空间枚举。
 */
export enum RedisStorageEnum {
  SESSION = 'session',
  SCRIPT = 'script',
  TASK = 'task',
}

/**
 * 系统支持的语言区域枚举。
 */
export enum LocaleEnum {
  ZH_CN = 'zh-cn',
  EN_US = 'en-us',
}
