// 性别枚举
export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
  SECRET = 'secret',
}

// 定义 Redis 服务名称
export enum RedisStorageEnum {
  SESSION = 'session',
  CAPTCHA = 'captcha',
  COS = 'cos',
}
